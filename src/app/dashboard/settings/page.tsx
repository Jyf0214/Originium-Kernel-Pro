import { SettingsPageContent } from './_components/SettingsPageContent';

/**
 * 设置页路由入口：实际逻辑全部移入 _components/SettingsPageContent。
 */
export default function SettingsPage() {
  return <SettingsPageContent />;
}
